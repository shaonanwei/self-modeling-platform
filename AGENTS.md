# AGENTS.md

作为 AI 助手参与本项目开发时，你必须：

- 动手前先看一看目标服务及其 API 模块的现有实现，模仿它的结构与风格通常是最快的路径
- 需要外部知识时，优先查阅 Spring Boot等官方文档，而不是凭记忆作答
- 需求不够清晰时，先复述已知信息、抛出关键澄清问题，再动手
- 面对跨服务或跨模块改动，拆成可验证的小步骤逐步推进比一气呵成更稳妥
- 风格不确定时，优先查阅现有服务并模仿当前业务域的实现方式
- 新增业务域时，**先建 API 模块再建 Service 模块**；API 模块只放契约，不引入 Service 依赖
- 与用户交互全程使用**中文**，代码注释和 Javadoc 亦使用中文

> 所有开发内容必须建立在深度思考过的基础之上，禁止机械生成与错误填充。
> 如果你已了解所有规范，请在用户第一次对话时说明："我已充分了解 java开发规范。"

---

## 开发规范

### 1.1 编写新功能前

1. 先阅读目标服务中已有的类，理解其结构、命名和风格
2. 主动模仿现有代码风格，包括缩进（Java 用 Tab，YAML/JSON 用 2 空格）、注解顺序、Javadoc 格式
4. 新建 Java 源文件必须包含 Apache 2.0 许可证头部和 Javadoc 类注释（`@author Chill`）

### 2.2 编写完成后

1. 使用 `mvn clean compile -DskipTests` 编译验证（父工程根目录执行会编译所有子模块），编译不通过必须修复
3. 除非用户明确要求，不应撰写示例或额外文档

---

##  数据库规范

- **表名前缀**：`smp_`（如 `smp_user`、`smp_role_menu`）
- **业务表必含字段**：`id`（BIGINT）、`create_user`、`create_dept`、`create_time`、`update_user`、`update_time`、`status`、`is_deleted`
- **主键策略**：雪花算法（`IdType.ASSIGN_ID`），Long 字段用 `@JsonSerialize(using = ToStringSerializer.class)` 防精度丢失
- **逻辑删除**：`is_deleted`（INT），`0` = 未删除，`1` = 已删除
- **索引命名**：唯一索引 `uk_` 前缀，普通索引 `idx_` 前缀
- **多数据库**：支持 7 种数据库，SQL 脚本位于 `doc/sql/`（全量建表 + 版本升级脚本），修改表结构时需同步所有脚本

---

## Codex 默认工作模式

### 轻量修复模式（默认）

除下述高风险场景外，本项目默认采用轻量修复模式：

- 先定位问题和影响范围，再进行最小必要修改
- 不默认创建设计文档、实施计划、Git worktree、子代理或额外示例文档
- 优先执行与改动直接相关的类型检查、单元测试或局部构建，不因小改动重复运行全量测试和全量构建
- 页面视觉效果、交互手感及业务验收可明确交由用户手工验证；Codex 仍需检查代码差异、语法/类型错误和能够低成本验证的客观问题
- 多个低风险小改动可以集中后，在提交、推送或发布前统一执行一次完整验证
- 除非用户明确要求，不自动提交或推送代码

轻量修复模式下，上述规则优先于“编写完成后必须执行全量 Maven 编译”的通用要求；应根据实际改动选择足够且最小的验证范围。

### 完整流程适用范围

遇到以下任一场景时，升级为完整流程，按需进行需求澄清、方案设计、TDD、分步实施和完整验证：

- 认证、授权、凭证、CORS、SQL 防护等安全相关改动
- 数据库结构、迁移脚本、事务、数据修复、数据源和数据库凭证相关改动
- 新增、编辑、保存、删除等核心数据保存逻辑及跨 Tab、跨步骤状态同步
- 跨模块重构、公共接口变更、架构调整或影响范围较大的功能
- 用户明确要求使用完整 Superpowers 流程
